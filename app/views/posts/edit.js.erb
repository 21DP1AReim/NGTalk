<% if @post.post_type == 'article' %>
  $('.container-for-post').html("<%= j render partial: 'edit_article', locals: { post: @post } %>");
  initializeTinyMCE();
<% else %>
  $('.container-for-post').html("<%= j render partial: 'edit_post', locals: { post: @post } %>");
<% end %>