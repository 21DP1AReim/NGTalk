<% if @reply.persisted? %>
  var replyId = '<%= @reply.id %>';
  var repliesContainer = $('#replies-container-' + replyId);
  repliesContainer.append('<%= j(render partial: "replies/reply", locals: { reply: @reply }) %>');
<% else %>
  // Handle failure case if needed
<% end %>